# Project-012 引き継ぎ記録

## 開発中AI調整（2026-09-17 — v0.1.9からの変更）

- AIのキャラクター・長さ・改行に関する指示を外し、通常のAIとして動作させる。
- AIの基本指示に「ため口でなれなれしい」「疑問形や質問で終わらず、返信の中で内容を完結させる」を追加した。
- AIの基本指示に「通常はすっとぼけたアホキャラ」「競艇に関する内容は非常に詳しく答える」を追加した。
- 返信の口調に「あほっぽい」を明示的に追加した。
- キャラクターの口調を「酔っ払いでぼんやりした、少し呂律のゆるいアホっぽい口調」へ調整した。
- Gemini出力上限を512トークン、応答待ちを12秒、ユーザーコメント上限を300文字へ変更した。フロントエンド、Edge Function、DBの入力制約を揃えている。
- 試験段階の投稿間隔を30秒から10秒へ変更。送信中の文字変更は外し、送信ボタンを薄く無効化して成功・失敗メッセージで状態を伝える。
- 投稿失敗の内部診断ログを追加。入力不正、モデレーション拒否、連投制限、設定不足、Geminiの401/403・429・5xx・タイムアウト・通信エラー・JSON不一致・MAX_TOKENS、返信検査、Supabase保存失敗を原因コードで切り分ける。コメント本文やAPIキーはログに出さない。
- 調査でSupabaseの返信保存RPCに残っていた60文字制限が原因の400エラーを確認。返信保存側の制限も解除し、長めのAI返信を保存できるようにした。
- 分類、肯定的コメントの30%抽選、おねだり返信の生成、JSON形式など、機能に必要な指示だけを残す。
- おねだり返信は肯定的コメントだけを対象にし、サーバー側の抽選確率を30%へ変更。否定的・中立・混合・判断困難、および深刻な悩み・金銭的困窮は0%のままにする。
- v0.1.9タグは変更せず、必要ならv0.1.9へ切り戻せる状態を維持する。

## 最新状態（2026-09-17 — v0.1.9）

- コメント欄の既存レイアウトを維持し、ユーザー投稿に先頭1文字の丸型アイコンを追加。ニックネーム未入力（匿名）は「匿」を表示する。
- ユーザーアイコンの色はニックネームから決定的に選び、ページ更新で色が変わらないようにした。匿名アイコンはグレーで固定する。
- AIタカシの返信には専用イラストを32pxで表示し、ユーザーアイコン（28px）より少し大きくした。全アイコンを左寄せで揃え、AI返信の時刻は表示しない。
- ユーザーコメントもアイコン右側の名前・時刻の下から本文が始まるように構造を統一し、AI返信と本文の開始位置を揃えた。
- AIタカシのサイト用アイコンを、よれた帽子・透明感のある紙コップの生ビール・自然に握った手を含む最新版へ更新。OFUSE側の画像設定は別途変更する。
- OFUSE案内リンクの表示を「🍺 タカシに生ビールをおごる」に更新。リンク先URLは変更しない。
- ユーザーとAIタカシのアイコンを40pxへ拡大。AIタカシのアイコンだけタップで拡大表示できるようにし、ユーザーの先頭文字アイコンは拡大操作を付けない。
- v0.1.8のタグ `e15ab78` は変更せず、問題があれば `git reset --hard v0.1.8` でmainと同じ状態へ戻せるようにしている。
- OFUSE設定用の1000×150px背景画像（PNG/JPG）を保存し、ダウンロードできるようにした。
- v0.1.9としてmainへ反映し、GitHub Pagesの公開URLを更新する。v0.1.8タグは切り戻し用に保持する。

## 最新状態（2026-09-15 — v0.1.8）

- 承認済みの2枚のUI画面案に合わせ、通常画面とコメント入力画面を実装。ルーレットの大きさ・質感・数字の中央配置、青いSTARTボタン、会場・レース選択、背景が見える余白、コメント一覧の一体型パネルを調整した。
- コメント入力は下部シート式に変更。入力欄と送信フッターを分離し、キーボード表示中も送信ボタンが見えるようにした。長文入力時は入力欄側だけがスクロールし、下書き・文字数表示・送信中の二重送信防止・Escape/背景タップによる閉じる操作を維持する。
- `docs/design/ui-reference-v0.1.7.png` と `docs/design/ui-comment-composer-v0.1.7.png` を再現基準として使用し、方針と確認範囲を `docs/design/UI-DESIGN.md` に追記。画像内のキーボードは固定実装せず、端末のOSキーボードを使用する。
- 320・390・430px幅、および入力中の縮小表示で横はみ出し、ルーレット停止後の表示、入力画面の送信ボタン位置、空白入力の拒否、下書き保持を確認。`node --check comments.js` と `git diff --check` が成功。
- v0.1.8をProject-012のmainへ反映し、GitHub Pagesで公開する。公開URLは `https://takashi7195.github.io/Project-012/`。

## UIデザイン基準 — 2026-09-15

- 今回の画面案を `docs/design/ui-reference-v0.1.7.png` として保存。
- キーボード表示中のコメント入力画面案を `docs/design/ui-comment-composer-v0.1.7.png` として保存。
- 画面案の方針と変更時の確認事項を `docs/design/UI-DESIGN.md` に記録。
- ルーレットを主役にしたコンパクトなスマホ画面、背景の見える余白、コメント入力時の下部パネル、キーボード直上の送信ボタンを今後のUI検討の基準とする。

## 最新状態（2026-09-15 — v0.1.7）

- OFUSE（https://ofuse.me/7de5342a）へ応援先を変更し、案内リンクの表示文を「AIタカシにジュースをおごる」に更新。AI返信の口調・抽選条件・安全確認・データ処理はv0.1.6から変更なし。
- GitHub Pagesのmainへ公開し、公開URLで表示とリンク先を確認する。

## 旧状態（2026-09-14 — v0.1.6）

- v0.1.6では、AIタカシの返信にチップ案内を追加。肯定的なコメントでは20%、軽い不満を含む否定的なコメントでは5%の確率で案内し、中立・混合・判定困難、深刻な悩み・金銭的困窮では案内しない。
- チップ案内返信は「チップ」「お金を送って」と直接求めず、コメントに合わせて軽く匂わせる。通常返信の「ため口・おとぼけ」方針を維持し、noteリンクは固定URLを使う。
- 抽選はサーバー側の暗号学的乱数で行い、`tip_requested` をコメントとともに保存。案内対象のコメントだけに「noteでチップを送る」リンクを表示する。
- Project-012のDB変更はSQL Editorで適用確認済み、comments Edge Functionも再デプロイ済み。ローカルNode.jsテスト14件成功。GitHub Pagesのmain最新ビルドは成功し、公開URLがHTTP 200・v0.1.6表示になることを確認済み。
- v0.1.6はGitHub mainのコミット `3c114d6` とタグ `v0.1.6` に保存。v0.1.5は発行していない。

## 2026-09-14 — v0.1.6: AIタカシの確率的なチップ案内

- 合意済みの確率・対象外条件に従い、通常返信候補とチップ案内返信候補を生成してサーバー側で抽選する。
- `tip_requested` をDBに保存し、該当するカードだけに固定のOFUSEリンクを表示する。
- ローカル検証: コメント関連Node.jsテスト14件成功。GitHub Pages APIからmain最新コミットのビルドを依頼し、ビルド成功と公開URLのHTTP 200・v0.1.6表示を確認。

## 2026-09-14 — v0.1.4

- v0.1.4では、AI返信を通常のAIに戻し、追加する口調を「ため口」「おとぼけキャラ」の2点だけにした。深刻な話題だけを別返信にする独自分岐と、返信内容のアプリ独自の話題別チェックを削除。投稿側のモデレーション、Gemini標準の安全制御、個人情報の伏字は維持する。
- 投稿側の安全確認を通過したコメントは、深刻な話題を含む場合も、伏字後の本文だけをGemini APIへ送って返信を生成する。ニックネーム、IPアドレス、会場・レース情報は送らない。無料枠のデータ取り扱いと深刻な話題も送信対象になることを `privacy.html` に記載。
- コメント投稿・Supabaseへの保存・一覧表示は従来どおり。DBの変更はなく、`reply_source` は `gemini` または `template` を使用する。Geminiが利用できない場合は定型文に戻る。
- バージョン表記をv0.1.4に更新し、HISTORY.mdに記録。comments Edge FunctionはSupabase Project-012へ再デプロイ済み。GitHub Pagesはmainブランチのrootから公開する。
- 「通常AI＋ため口・おとぼけ」の返信方針をv0.1.4に含め、コメント試験で返信表示を確認した。
- 確認: コメント関連Node.jsテスト10件成功、Node.js構文チェック、`git diff --check`、Supabase再デプロイ成功ログ、GitHub Pages run #32成功、公開URLでHTTP 200とv0.1.4表記を確認。コメント試験で返信表示も確認済み。

## 2026-09-14 — AIタカシのチップ案内を実装・公開準備

- ユーザー合意: 肯定的なコメントは20%、軽い不満を含む否定的なコメントは5%の確率でチップ案内返信を選ぶ。中立・混合・判定困難は0%。深刻な悩みまたは金銭的困窮が含まれる場合は、肯定/否定を問わず0%。
- 返信ではチップを直接求めず、おねだりを軽く匂わせる。題材や言い方を固定せずコメントに自然につなげ、直下の固定リンク「noteでチップを送る」で行き先を示す。AIに過去の返信履歴は渡さないため、同じ表現が再び出る可能性はある。
- AIの通常返信の指示は「通常AI＋ため口・おとぼけ」の2点を維持。Geminiを1回呼び出し、分類・通常返信・チップ案内返信をJSON構造化出力で得る。Edge Functionが暗号学的乱数で確率を引き、通常返信またはチップ案内返信を選ぶ。不正出力やAPI失敗はチップ案内なしで従来の定型文へ戻す。
- `tip_requested` をDBに保存するmigrationとRPC変更を追加。Project-012 SQL Editorで適用し、`tip_requested` 列、新旧RPC両方の存在を読み取りクエリで確認。既存コメントは全件 `false`（チップ案内0件）で、旧4引数RPCも残してEdge Functionの切替まで投稿を継続できる。`true` のカードだけに固定のOFUSEリンク「AIタカシにジュースをおごる」を表示し、URLはAIに生成させない。
- `privacy.html`、`docs/COMMENT-MODERATION.md` を更新し、抽選確率・対象外条件を記載。画面案ではサンプルの先頭カードにだけチップリンクを表示する。
- ローカル検証: コメント関連Node.jsテスト14件成功（判定率境界、対象外条件、個人情報伏字、無効出力のfallbackを含む）、`comments.js` とAI返信・モデレーションモジュールのNode構文チェック、`node --experimental-strip-types --check supabase/functions/comments/index.ts`、`git diff --check` 成功。Deno型チェックはDeno未導入のため未実施。`http://127.0.0.1:8012/comments-integration-preview.html` を開き、サンプルの先頭カードにチップ案内文とnoteリンクが表示されることを確認。このプレビューはサンプル専用で本番APIへ接続しない。実Gemini試験で分類表示を確認し、試験ページのCSSで `hidden` 属性が上書きされる表示不具合を修正。肯定20%・不満5%の抽選のため、少数試験で案内が一度も表示されない場合があることも確認。試験ページに抽選確率を明記。
- 実返信の手動試験用にローカル限定の `ai-reply-test.html` と `tools/start-ai-reply-test.ps1` を追加。公開リポジトリには含めず、同じWi-Fi上のスマートフォンから `192.168.11.20:8013` で開ける。試験APIはPowerShell起動ごとに作る6桁の一時アクセスコードで保護し、誤入力8回で停止する。Geminiへ1回ずつ接続するが投稿保存は行わない。「試験サーバーを停止」ボタンで終了し、操作がない場合は30分で自動停止する。APIキーはPowerShellの非表示入力からプロセス環境へ渡し、ファイルやブラウザーには保存しない。HTTP画面・アクセスコード保護・入力チェック・停止操作の疎通はダミーキーで確認済み。実Geminiの分類表示をスマートフォンから手動確認済み。
- 公開作業中: Supabase migrationはSQL Editorで適用済み（CLI `link` は権限エラーのため、CLI migration履歴には未登録）。コメントEdge FunctionはProject-012へ再デプロイ済み。GitHub Pagesは未更新。バージョン表記はv0.1.4のまま。バージョン番号・タグはユーザー指定まで変更しない。

## 2026-09-14 — v0.1.3

- v0.1.3を正式版としてGitHubのmainへ反映。Project-012のmainをこの版の公開基準とし、このコミットのGitHub Pages自動デプロイが成功したことを確認済み。
- v0.1.3は、v0.1.2のルーレット改善を維持し、コメント初回版を加えた版。艇色、抽選ロジック、待ち時間、停止演出は変更していない。
- コメント欄とSupabase Edge Functionは公開済み。利用者による投稿、コメントDBへの保存、公開一覧への再表示、AIタカシの定型返信を確認済み。確認時点で公開コメントは2件あり、どちらも visible 状態で返信とともに保存されていた。
- 2026-09-14にGemini返信を本番有効化。Supabaseに `GEMINI_API_KEY` をsecret登録し、Edge Functionを再デプロイ。公開サイトからの通常コメント1件で、AIタカシのGemini返信が表示され、DBの `reply_source=gemini`、`status=visible` を確認した。
- AIタカシの会話は小学4年生にも伝わる短い言葉とため口にし、通常時は大きくとぼけた明るいボケを返す。深刻な内容は従来どおり親身な定型文へ切り替える。変更はコミット `bd41984` でmainへ反映し、Supabase RPCの許可値をSQL Editorで更新、comments Edge Functionを再デプロイ済み。新しい本番投稿での口調確認は未実施。
- Gemini用DB変更は、SQL Editorで `reply_source` 列とservice_role専用RPC `create_comment_with_reply` を追加して適用。SQL Editorで `supabase_migrations.schema_migrations` が存在しないことを確認したため、CLI migration履歴には登録されていない。CLI `link` はscoped tokenで権限エラーが続いた。今後のDB変更でCLIを使う場合は、migration履歴とリンク権限を先に整える。
- 連投制限キーはHMAC形式で保存され、生IPはコメント用DBに保存しない。15分ごとの削除ジョブは稼働し、直近の実行成功も確認済み。
- ローカルの低コスト確認: Node.js構文チェック、コメント安全判定とGemini返信テスト計10件、git diff --checkが成功。Denoが未導入のため、Deno型チェックは未実施。
- 削除依頼窓口、通報、投稿者自身の編集・削除、専用管理画面、深刻な内容の本番経路確認、実レース情報APIは残課題。自動伏字・安全判定は完全ではなく、運営者による定期確認が必要。
- v0.1.3公開時点では作業用PCのローカルGitが未同期で未コミット変更があったが、2026-09-14にGitHub main（`a6ccb2b`）へfast-forward同期した。同期前の変更は確認用にGit stash `pre-sync Project-012 v0.1.3` へ保全している。
- 以下の古い節は、それぞれの日付時点の状態を記録したもの。後の日付の記録が状態を更新する。

## 2026-09-14 — Gemini無料枠を使うAIタカシ返信を本番反映

- ユーザー承認により、Google Gemini API無料枠を使う試作を追加。モデルは公式料金表で無料枠が掲載されている `gemini-3.1-flash-lite`。利用可否や上限はAPIキーに紐づくGoogleプロジェクトとモデルの制限に従う。
- 投稿本文はサーバー側で個人情報を伏字にした後、通常コメントに限りGeminiへ送る。ニックネーム、IPアドレス、会場・レース情報は送らない。APIキーはSupabase Edge Functionの `GEMINI_API_KEY` secretで扱い、ブラウザーには出さない。
- 深刻な悩み・危険を示す表現は外部送信せず、親身な定型文で返信する。API失敗・不適切・長すぎる出力は従来の定型返信へフォールバックする。保存時に `reply_source` へ `template` / `gemini` / `empathetic` を記録する。
- `privacy.html` にGemini無料枠のデータ利用と人による確認の可能性を明記。
- 試作コード・テスト・Gemini送信を説明する利用案内をコミット `a3003c3` でmainへ反映。新しい専用GoogleプロジェクトのAPIキーで最小のGenerateContentリクエストが成功し、無料枠で応答が返ることを確認した。後続作業でSupabase SQL EditorにGemini用DB変更を適用し、Edge Functionを再デプロイ、`GEMINI_API_KEY` をSupabase secretへ設定した。公開サイトからの通常コメント1件でGemini返信とDB保存を確認済み。深刻な内容の本番経路、API障害時の本番fallbackは未検証。APIキーはチャットへ貼らずSupabase secretで管理する。

## 2026-09-14 — AIタカシの口調を調整して本番反映

- 通常時は小学4年生にも伝わる簡単な言葉、ため口、明るく大きくとぼけるボケを使う。特定の人物そのものを再現するのではなく、軽妙で適当なお調子者という一般的な特徴に置き換える。
- 深刻な悩み・喪失・危険・被害を示すコメントにはボケを使わず、親身な定型文を返す。
- Geminiが丁寧語、長文、個人情報、危険な内容などの条件に合わない返信をした場合は採用せず、ため口の定型文10種類から返す。
- `create_comment_with_reply` が `template` を受け入れるSQL migrationを追加し、Supabase Project-012 SQL Editorで適用。既存の入力検証とservice_role限定の権限は維持した。
- コミット `bd41984` をGitHub `main` へpushし、comments Edge FunctionをProject-012へ再デプロイした。定型文、Gemini生成文、深刻な内容向け返信をそれぞれ保存できる構成。
- 検証: コメント関連Nodeテスト10件成功。Node構文チェックと `git diff --check` も成功。Deno型チェックはDeno未導入のため未実施。再デプロイの成功ログを確認した。新しい本番コメントは追加しておらず、変更後の返信口調とDB `reply_source` の実投稿確認は次回の自然な投稿時に行う。

## 2026-09-14 — v0.1.4: 通常AIに口調を2点だけ加える

- 通常のAI応答に加える設定を「ため口」「おとぼけキャラ」のみにする。特定のボケ型や、小学4年生相当の語彙、毎回冗談を入れる指示はしない。
- 深刻な内容を独自判定して別返信にする処理と、返信文へのアプリ独自の話題別チェックを削除。コメント投稿側のモデレーション、伏字処理、Gemini標準の安全制御は維持する。
- 投稿側の確認を通過した深刻な話題を含むコメントも、伏字後の本文がGoogle Gemini APIへ送信される。Gemini API無料枠ではGoogleによるサービス改善利用や人手確認の可能性があることを、公開前に利用案内へ記載。
- Gemini生成返信の利用不可・エラー時は10種類の定型文へフォールバックする。生成返信には1行60文字以内と個人情報の再掲防止を適用する。

## 2026-09-12 23:26 JST — v0.1.1 艇色とルーレット数字の中央配置を調整済み

### 初期時点の目的と構成（2026-09-12）
- 競艇に慣れた利用者向けの、スマートフォンで遊ぶ3連単抽選ゲーム「AIタカシの3連単予想」。
- HTML・CSS・JavaScriptと画像による静的サイト。ビルド不要。Supabase、外部データベース、コメント保存処理は使っていない。
- 会場別の固定統計データから、IPFで120通りの重複しない3艇の組み合わせの確率を求めて抽選する。実際の出走表・選手情報の取得や外部AIによる予想は未実装。
- 24会場と1R〜12Rを選択できる。現在、レース番号は抽選計算に使用していない。
- 作業ディレクトリ: `C:/codex/project-012/Project-012`
- GitHub: https://github.com/takashi7195/Project-012
- 公開サイト: https://takashi7195.github.io/Project-012/
- GitHub Pagesはmainブランチのルートを公開する設定。

### 002から012への独立コピー
- Project-002のmain `ace9acecda5c434039c6ffe09eae1fc159f22d65`をコピーし、012専用リポジトリを作成した。
- 002は変更していない。012のGit接続先は012のみで、自動同期はない。
- Supabaseを利用していないため、データベース分離・データ移行は不要だった。
- 元の開発履歴 `docs/HISTORY.md` はコピーしたまま保持。本ファイルは012の新しい引き継ぎ記録。

### 維持する設計意図（ユーザー確認済み）
- 左から1着・2着・3着であることは利用者の共通認識。着順ラベルや重複する確定結果表示は追加しない。直感的に分かる説明を重ねない。
- マークシートを手に取ってから記入が間に合う待ち時間を確保し、最後の1着が決まるまでの期待・焦れを演出する。
- 停止順は右（3着）→中央（2着）→左（1着）。待ち時間は順に3秒・6秒・9秒で、それぞれの停止アニメーション時間も加わる。短縮やスキップは希望されていない。
- 競艇の雰囲気を出す背景を重視する。下部余白は将来のユーザーとAIタカシのコメント欄の候補であり、不要な説明で埋めない。
- 修正は1つずつ確認する。公開前にローカル画面で見た目を確認する方針。

### 2026-09-12時点で公開済みの改善
- 公開mainのコミット: `d7b128d725e02559c32ca6949ff8033933ca3c51`。
- 文字・選択欄・リールが端末幅に合わせて伸縮するレスポンシブ表示、セーフエリアを考慮した余白を導入。
- リールの確定位置をピクセル固定から高さの倍数へ変更し、画面幅変更後の数字のずれを改善。
- 背景を元の1536×1024の構図・解像度を保ったWebPに変更。2,397,550バイト→314,858バイト（約87%削減）。元のPNGも保持。
- ブラウザで320・390・430px幅と横画面、横はみ出し、抽選中・確定後の幅変更を確認した。スマホ実機での網羅的検証は未実施。

### v0.1.1として保存する変更（まだ未公開）
- 数字を専用の `span.boat-number` に分け、Arial/sans-serif、`line-height: 1`で表示。数字サイズ・リールの外枠と高さは維持。
- 当初試した上方向の補正は、ユーザーから上すぎるとの指摘を受けて除去。現在は上下方向のtransform補正なし。
- ユーザー指定の公式オッズページの計算済み背景色を確認し、以下へ変更した。
  - 1号艇: `#EBEBEB` / 黒文字
  - 2号艇: `#505050` / 白文字
  - 3号艇: `#F04141` / 白文字
  - 4号艇: `#4184D0` / 白文字
  - 5号艇: `#ECE962` / 黒文字
  - 6号艇: `#54B74D` / 白文字
- 参照: https://www.boatrace.jp/owpc/pc/race/odds3t?rno=12&jcd=02&hd=20260912
- 上記は参照ページの使用色であり、競艇全体の統一色規格と断定しない。
- ローカル画面でユーザーから「いいかんじです」と確認を得た状態を保存する。今回の数字・色変更では停止処理や待ち時間を変更していない。
- 保存方法: 専用ブランチ `checkpoint/v0.1.1` と注釈付きタグ `v0.1.1`。mainは更新せず、GitHub Pagesの公開版を維持する。
- 構文確認は `node --check script.js`、差分確認は `git diff --check`。これらは動きの完全な検証を意味しない。

### 2026-09-12時点の残課題・今後の構想
- 停止直前に不自然に加速する問題は未修正。通常回転の「1秒で6艇分」と、停止処理の固定600px/sが不一致。停止用cubic-bezier(0.2, 0.8, 0.3, 1)の開始速度も連続していない。通常回転の速度から連続して減速する設計が次の検討対象。
- 停止処理を変える際は、待ち時間と停止順を維持し、正しい数字への停止、逆走防止、繰り返し動作、画面幅変更を確認する。
- 実レースの情報を取得して予想するAPI連携は将来希望。非公式の候補 https://github.com/boatraceopenapi/api は出走表・直前情報・結果を提供し、説明上は約3分間隔更新。採用時は最新の利用条件、更新遅延、欠損・品質を確認する。まだ接続していない。情報取得APIと予想ロジックは別途設計が必要。
- ユーザーとAIタカシのコメント欄は案の段階で、実装・DB作成は未依頼。
- プレビューは `http://127.0.0.1:8012/`。同じPC上でローカルサーバーが動作中のみ利用できる。必要時はプロジェクトルートで `python -m http.server 8012 --bind 127.0.0.1` を起動する。
- 公開サイト用QR画像はリポジトリ外の `C:/codex/project-012/project-012-qr.png` に生成済み。

## 2026-09-12 23:35 JST — v0.1.1の公開とバージョン表記追加

- 前項の保存後、ユーザーの公開依頼により、保存済みv0.1.1をmainへfast-forwardで反映してGitHubへpushした。GitHub Pagesの公開成功（built）を確認済み。前項の「まだ未公開」は保存時点の状態であり、現在は公開済み。
- 公開URL: https://takashi7195.github.io/Project-012/
- ユーザーは開発者が気づける程度の控えめなバージョン表記を希望し、STARTボタン下の右端に置く案を承認した。
- `index.html`に `v0.1.1` を表示する要素を追加。`style.css`で文字サイズ11px、文字色 `#777`、上余白8px、右揃え、行の高さ1.2を指定。枠・背景・説明文は追加していない。
- 表記追加コミット: `e32a16295598340d974624b776b788d0f03dff22`。mainへpushし、このコミットのGitHub Pages公開成功（built）を確認した。
- 保存済みタグ `v0.1.1` とブランチ `checkpoint/v0.1.1` は引き続き `1e9b898f4b3830bd817b76b80ed631cbaf764bc2` を指す。タグは付け替えていないため、タグの保存内容には後から追加した画面上のバージョン表記を含まない。表記込みの版は上記コミットで参照できる。
- 現在の作業ブランチはmain。停止動作の改善は未着手で、停止順・待ち時間・予想ロジックは変更していない。
- 今回のユーザー依頼「記録してください」により本項を追記。既存の記録は変更・削除していない。

## 2026-09-13 10:06 JST — ルーレット停止の減速処理を修正・公開

- 前項で未着手としていた停止動作の改善を実施した。ユーザーの公開依頼を受け、mainへ保存・公開済み。
- 公開コミット: `fcd464b491bb56d90b6a5e9d49642cde7f949b52`。GitHub Pagesの最新ビルドが同コミットで `built`、エラーなしであることを確認した。
- 公開URL: https://takashi7195.github.io/Project-012/

### 修正内容
- `script.js`の停止処理を変更。CSS回転から停止用transitionへ切り替える方式を、現在位置を引き継いでrequestAnimationFrameで等減速する方式へ置き換えた。
- 位置・距離を艇1個分の高さを単位として扱い、通常回転の6艇/秒と減速開始速度を一致させた。固定600px/sと停止用cubic-bezierによる速度の不連続を解消する設計。
- 停止位置が近すぎる場合は1周分追加し、3〜9艇未満の減速距離を確保。減速時間は距離から算出して約1〜3秒。進行率に対して `p * (2 - p)` を用い、速度が一定の割合で0へ下がる。
- リールの表示位置を同じ数字が並ぶ中央の周回に折り返し、確定時は誤差を除いた位置へ合わせる。画面幅変更後も高さに追従する。
- 3着→2着→1着の順と、減速開始前の待ち時間3秒・6秒・9秒を維持。各待ち時間は直前リールの停止完了後から数えるため、減速時間が別途加わる。全体の所要時間は従来より長くなる場合があることを説明済み。
- 艇色・数字の中央配置・背景・抽選確率の計算は変更していない。

### 検証と公開
- `node --check script.js` と `git diff --check` を通過。
- リポジトリ外の検証スクリプト `C:/codex/project-012/check-roulette.cjs` で、601通りの開始位置×6艇＝3,606ケースを検証。開始速度の一致、減速中の単調な速度低下・前進、着地艇番、折り返し位置の範囲を確認した。数学的検証は実機の滑らかさを保証するものではない。
- ローカルブラウザで2回の抽選完了、STARTボタンの復帰、スマホ幅での結果表示と幅変更後の表示を確認した。スマホ実機での感触や低性能端末での処理落ちは未検証。
- ユーザーの「公開サイトへ反映してください。その後URLをQRコードで表示してください」に従って公開し、既存の公開URL用QR画像を再表示した。
- 保存済みタグ `v0.1.1` と `checkpoint/v0.1.1` は変更していない。今回の停止処理はタグに含まれず、上記公開コミットで参照する。画面上のバージョン文字列は引き続き `v0.1.1` で、新しい番号・タグは未設定。
- 今後はユーザーのスマホで停止の感触を確認し、必要なら減速距離・時間を調整する。レース情報API連携とコメント欄は引き続き未実装の構想。
- 今回の記録依頼により追記。既存の記録は変更・削除していない。

## 2026-09-13 10:12 JST — v0.1.2 ルーレットの減速停止を保存

- ユーザー依頼により、ルーレットの滑らかな停止処理を含む現在の状態をv0.1.2として保存する。
- `script.js`では、通常回転の6艇/秒から同じ速度で減速を始め、requestAnimationFrameで等減速する。停止目標まで艇単位で3〜9艇未満進み、正しい艇番へ止める。
- 停止順（右3着→中央2着→左1着）と、各リールの減速開始前の待ち時間（3秒・6秒・9秒）を維持。減速時間は別途加わる。
- 検証: `node --check script.js`、`git diff --check`。601位置×6艇の3,606計算ケースで開始速度、単調な減速、前進、停止艇番、周回の表示範囲を確認。ローカル画面で2回の抽選完了、スマホ幅の表示も確認した。スマホ実機・低性能端末での体感は未検証。
- ページの控えめな版番号を `v0.1.2` に更新し、この保存版のコードと一致させる。
- 今回は専用ブランチ `checkpoint/v0.1.2` と注釈付きタグ `v0.1.2` へ保存する。公開ブランチmainには変更を反映しない。従って公開サイト表示は引き続きv0.1.1のまま。
- v0.1.1のタグと履歴は変更しない。今回の保存コミットをv0.1.2の基準とする。
- 艇色・抽選ロジック・背景画像・コメント機能・実レースAPIは今回変更しない。API連携とコメント欄は未実装。

## 2026-09-13 10:24 JST — v0.1.2をmainとすることを確認・記録

- ユーザーが改めてv0.1.2をProject-012のmainにするよう依頼。確認時点でmainはすでにv0.1.2のコミット `4288a5e4d96da53e89c161e5da20c8c4ed5b9791` を指し、origin/mainと一致していた。
- GitHub Pagesの最新ビルドも同コミットで `built`、エラーなし。したがってmainや公開ソースの再変更は不要だった。
- `v0.1.1`タグ（注釈タグオブジェクト `e6d602117dda729a344995d67bf435f706ff0cd6`）と`v0.1.2`タグ（`a7a9d8da531b942208cfefe9635d74fb78ef8df0`）の両方を維持する。タグはいずれも同名バージョンの保存済みコミットを指す。
- 今回の依頼により、v0.1.2がmainおよび公開版であることを明記する。main内容に変更を加えず、記録のみを追記。

## 2026-09-13 12:03 JST — コメント初回実装に向けた運用ルールを記録

- ユーザー確認により、初回コメント機能のルールを `docs/COMMENT-MODERATION.md` に明文化した。
- ニックネーム任意、ログインなし、本文280文字まで、安全チェック後すぐ公開とする。
- 個人情報はコメント全体ではなく該当箇所を通知なしで伏字にする。ユーザーの追加指定により、部分桁・ユーザー名・ドメイン・地域名など特定につながる情報を残さず、`[電話番号]`・`[メールアドレス]`・`[住所]` のように種類だけ示す形式へ変更した。さらにLINE IDやSNSアカウント名・IDも伏字対象に加え、`[SNSアカウント]` と表示する。先に挙げた部分伏字例はこの指定で置き換える。伏字前の情報はデータベースへ保存しない。
- NGワードの扱いを追加確認。単語だけで投稿を止めず、曖昧な表現・強い感情・通常の批判や冗談は原則許可する。人への具体的な脅迫や危害の呼びかけ、個人を特定できる情報をさらす行為、連絡先や支払い方法を伴う詐欺・金銭勧誘など具体的な被害につながる場合に限定して止める。判定文言と誤検出の扱いは実装時に確認する。
- 投稿を安全判定で止めた場合の表示は、詳細な理由を説明せず「コメントを投稿できませんでした」とする。
- 初回は利用者向け通報機能とサイト内の専用管理画面を作らない。運営者がSupabase管理画面から投稿を確認・非表示・削除し、管理アクセスは運営者に限定する。
- コメント本文に自動削除期限は設けず、コメント欄の履歴として役立つ間は保存する。運営者が定期確認し、不要・問題のある投稿を削除する。公開・保存の目的を利用者へ知らせ、連投対策用のIP等の技術情報は本文と分けて必要最小限の期間だけ保持する。具体的な項目と期間はSupabase実装時に決める。
- 投稿者自身の編集・削除は初回に設けず、削除依頼は連絡窓口で受け付け、運営者がSupabaseから削除する。連絡窓口の種類と案内場所は公開前に決める。
- 管理者へ非公開メッセージを送る専用ページは後日実装する残課題として記録し、初回には含めない。コメント機能の公開前に、削除依頼を受け付ける別の連絡方法を定める。
- 投稿フォームの近くに公開されることを短く知らせ、利用目的・個人情報の扱い・保存と削除依頼方法はプライバシー案内にまとめる方針とした。案内ページは実際の保存データと削除依頼窓口が決まった後、コメント機能と合わせて作る。
- 公開用メールアドレスがないため、削除依頼窓口の決定を残課題とする。専用の非公開メッセージページ等を後日検討し、削除依頼窓口とプライバシー案内が整うまでは実コメント機能を公開しない。
- 「死」「殺す」など単語単体は一律に伏字にせず、明確な脅迫・自傷を促す内容は公開しない。
- 外部リンク・連絡先誘導・予想販売・送金要求など詐欺や勧誘につながる投稿を公開しない。単語ひとつではなく勧誘の文脈を対象とする。
- 通報機能と本物のAI返信は初回対象外。通報機能は後続課題とし、初回は運営者がSupabase上で確認・非表示・削除する運用を前提とする。
- これは仕様の記録であり、Supabase接続や実投稿機能の実装ではない。Project-012は引き続き静的サイトである。
- ユーザーの追加方針として「楽しいサイトであること」と「安全性を最優先すること」を最上位の設計原則にした。犯罪・違法行為の助長、人権侵害、法令違反を避ける設計・運用を目指し、コメント機能と将来のAI返信の両方に適用する。
- 初回は本物のAI生成ではなく、競艇に合う短い定型文を10個程度用意し、ランダムに返信する方針とした。同じ返信が続くことを避け、個別に理解したとの誤解、舟券勧誘、的中保証、危険・違法な内容を含めない。実際のAI生成へ移行するための安全ルール・提供元・費用等は将来課題。
- 初回の返信表示名は「AIタカシ」とする。返信内容は事前に用意した定型文からランダムで選ぶ。コメント画面案の返信ラベルにも反映した。
- ユーザー確認により初回の定型返信10文を決定し、コメント画面案のサンプル返信を差し替えた。実際の投稿に対するランダム選択と連続重複の防止はコメント機能実装時に行う。
- 本体ゲーム画面とコメント欄を縦につないだローカル確認用 `comments-integration-preview.html` を作成。実ゲームのルーレット、コメント入力欄（本文280文字上限）、サンプル100件を表示する。投稿保存・Supabase接続はなく、公開サイトの `index.html` は変更していない。
- コメント一覧は新着20件から始め、末尾までスクロールしたら次の20件を自動取得する。「もっと見る」ボタンは置かず、取得中だけ末尾に最小限の `loading...` を表示する。自動取得で画面位置は動かさない。
- ログインなし投稿の連投対策として、同じ利用環境からの投稿を30秒に1件までに制限する。安全性と迂回耐性を保てる具体的な実装方法は、バックエンド設計時に決める。

## 2026-09-13 — コメント実投稿機能の初回実装

- ユーザー方針に従い、既決の最低限の安全対策を使って、実投稿機能の初回版を実装する。通報・専用管理画面・本物のAI返信などは後続課題とする。
- 既存の100件サンプル画面を本番用コメント画面へ置き換え、任意ニックネーム、280文字入力、投稿公開の案内、投稿状態表示を用意。新着20件から表示し、末尾近くで次の20件を自動取得する。
- `comments.js` はSupabase Edge Functionを呼び出す。投稿の追加・一覧表示ではDOMの `textContent` を使い、投稿文をHTMLとして解釈させない。
- Edge Functionのソースでは本文サイズ制限、投稿文字数、サーバー側の個人情報伏字、明確な脅迫・送金勧誘の最低限判定、同一IP由来の30秒制限を実装する。生IPは保存せず、service-roleキーでHMAC化した値だけを24時間保持する。DBのテーブル権限はservice_roleだけに限定する設計。
- Project-012のSupabaseで `comments`、`comment_rate_limits`、`comment_reply_state` テーブルを確認。コメントテーブルは空で、Edge Functionはまだデプロイされていない。
- SupabaseダッシュボードのEdge Functionエディターに `comments` 関数と `moderation.mjs` の下書きを準備したが、公開デプロイはしていない。
- `node --test supabase/functions/comments/moderation.test.mjs` は4件成功。`node --check comments.js`、NodeのTypeScript構文確認、`git diff --check` も成功。
- 390×844のスマートフォン幅で入力欄・文字数・投稿ボタンのレイアウトを確認。背景も残り、横はみ出しなし。Edge Function未公開のためローカル画面では一覧読み込みエラーを確認し、再読み込み操作を表示する。
- 公開用Supabase接続と投稿機能はまだ有効化していない。削除依頼窓口とプライバシー案内の公開条件が未解決であり、公開サイトには現在もサンプルのみが表示される。

## 2026-09-13 — コメント機能の段階公開方針

- ユーザー確認により、削除依頼窓口は残課題として、実投稿機能を先に公開する方針に変更。
- `privacy.html` を作成し、コメントが公開・保存されること、任意ニックネーム、定型返信、個人情報伏字の限界、Supabaseでの処理、連投制限用データの保持、投稿者自身の編集・削除機能がないこと、削除依頼窓口が未設置でサイトから個別依頼を送れないことを明記。
- コメントフォームから同案内へリンクする。削除窓口がない現状を伏せたり、受付可能と誤解させたりしない。
- 公開実行前の確認事項: Edge Functionの本番デプロイと公開呼び出し設定、GitHub Pagesへの反映、実機相当の表示・読込・投稿確認。

## 2026-09-14 — コメント公開前の送信・ログ確認

- Supabaseの公式資料を確認し、関数のInvocationログにはリクエスト／レスポンス情報（ヘッダー・本文を含む）が記録される場合があるため、本文と接続情報がSupabase側ログに含まれる可能性を `privacy.html` と `docs/COMMENT-MODERATION.md` に追記した。確認時点のProject-012はFreeプランで、プラン別ログ保持期間は1日。Supabase側ログはコメント用DBとは別管理。
- 通常のブラウザー投稿では、送信前に電話番号・メールアドレス・住所・LINE ID・SNSアカウントを伏字にする。サーバー側でも再度適用する。個人情報を入力しないよう求める案内と、自動検出の限界も維持する。
- ブラウザー側とサーバー側の伏字結果を代表例で照合するテストを追加。安全判定テストを含め5件すべて成功。JavaScript・TypeScript構文とPowerShellデプロイスクリプトも確認済み。
- Project-012の公開用publishable keyをブラウザーの `apikey` ヘッダーに設定。RLSとテーブル権限はservice_role限定で、service_role keyはブラウザーへ含めない。
- コメントEdge Functionの本番デプロイとGitHub Pagesへの反映はまだ。公開サイトには未反映で、API稼働確認後に同じ変更一式をmainへ反映する。

## 2026-09-14 — コメント初回版を公開

- Supabase Project-012へ `comments` Edge Functionをデプロイ。
- コメント画面、サーバー処理、個人情報伏字・最低限の安全判定、プライバシー案内、実装記録をGitHubの `main` へ反映。GitHub Pagesのビルド成功を確認。
- 公開URL: `https://takashi7195.github.io/Project-012/`。コメント欄へのQRコードを発行。
- 公開ページからコメント一覧が取得でき、空の状態表示になることを確認。動作確認用の投稿は公開データに残さないため、本番での投稿・DB保存の成功は未確認。
- 削除依頼窓口、通報機能、投稿者自身の編集・削除、実AI返信、専用管理画面は合意どおり後続課題。自動安全判定は最低限であり、すり抜けや誤判定の可能性を利用案内に記載。

## 2026-09-14 — 履歴と次回開発課題の監査

- 冒頭に最新状態を追加し、旧節がそれぞれの時点の記録であることを明記。2026-09-12時点の「Supabase未使用」「コメント未実装」などを現在の状態と誤認しにくくした。
- 2026-09-13のv0.1.2関連記録にあった10:24と10:12の順序逆転を修正。
- コードと運用記録を照合。定型返信10種類・連続重複防止、30秒連投制限、24時間後の制限キー削除ジョブはソースにある。公開サイトの一覧取得、Supabase関数のデプロイ、GitHub Pagesビルド成功は確認済み。本番での実投稿とDB保存は未確認。
- `moderation.test.mjs` の5件はすべて成功。JavaScript/TypeScript構文検査と `git diff --check` も実施する。

### 次回以降の開発課題（優先順）

1. 先にローカルGitをremote mainと同期する。`git fetch` がローカル環境で動かず作業ツリーに未コミットファイルもあるため、状態を保持したまま同期する。
2. 利用者向けに表示を残さない方法を用意してから、本番の投稿・DB保存・一覧再取得を一度通しで確認し、確認用データを確実に削除する。現時点では公開コメントを作るため未実施。
3. 削除依頼の窓口・運用方法を決め、案内ページを更新する。現在は窓口がなく、利用者はサイトから個別の削除を依頼できない。
4. Supabase管理画面でコメントを定期確認し、問題投稿を非表示・削除する運用を決める。通報ボタンや管理画面はまだない。
5. 個人情報伏字と脅迫・詐欺判定は限定的なパターンであり、すり抜け・誤判定がある。住所表記の揺れ、電話・SNS表記の揺れ、勧誘の言い換えを追加検証する。
6. `pg_cron` のジョブがSupabase上で実行され、24時間を過ぎた連投制限キーを削除していることをダッシュボードで確認する。
7. Supabase Project-012は確認時点でFreeプラン。Supabaseの現行案内では、7日間の利用が少ないFreeプロジェクトは一時停止される場合がある。通常の利用頻度で維持できるか、停止を許容するか、有料プランにするかを決める。
8. Android ChromeとiOS Safariの実機で、投稿・伏字後の表示・一覧の追加読み込みを確認する。過去の画面案だけでは本番投稿動作や両OSの実機確認を代替できない。
9. 本物のAI返信、通報、投稿者自身の編集・削除、管理者への非公開メッセージ、実レース情報APIを必要な順で設計する。
