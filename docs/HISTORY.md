### 2026-08-21 10:00
競艇3連単予想ゲームの新規作成を開始。
設計案（プロジェクト構成、ファイル構成、ゲームロジック）を提示し、ユーザー承認を得た。
実装を開始する。

### 2026-08-21 10:30
プロジェクト構成を整理。
- HISTORY.mdをdocs/へ移動
- README.mdの作成
- .gitignoreの作成（GEMINI.mdを除外）

### 2026-08-21 11:00
AI3連単ゲームへ改修。
- UIのシンプル化、ルーレット演出の実装
- 会場別重み付け抽選ロジックの実装
- README.mdの更新

### 2026-08-21 11:30
IPFアルゴリズムによるAI3連単生成ロジックの導入。
- 艇国DBの周辺分布から3連単結合確率を推定し、サンプリングに利用。
- 統計的な検証関数 runValidation を追加。

### 2026-08-21 12:00
ルーレット演出の停止順序を修正。
- 停止順を「右(slot-3:3着)→中央(slot-2:2着)→左(slot-1:1着)」となるようロジックを修正。

### 2026-08-21 12:30
背景画像の設定。
- Copilot_20260821_224628.pngを背景に適用。
- コンテナに半透明の背景色を追加し可読性を確保。

### 2026-08-21 13:00
ルーレット演出停止順序の再調整。
- 停止順を「右(slot-3:3着)→中央(slot-2:2着)→左(slot-1:1着)」となるよう、要素マッピングを修正。

### 2026-08-21 13:30
レイアウトのセンター配置調整。
- flexboxを用いてコンテンツを画面中央に配置するよう style.css を修正。

### 2026-08-21 14:00
レイアウトの微調整。
- コンテンツを画面中央より少し上に配置するように style.css を修正。
- slot内の数字を中央揃えにするよう flexbox を適用。

### 2026-08-21 14:30
レース番号選択機能の追加。
- 会場選択の横に「第1R～第12R」選択プルダウンを追加。

### 2026-08-21 14:45
ルーレットの停止タイミング調整。
- 停止までの待ち時間を「右(3着)3秒、中央(2着)6秒、左(1着)9秒」に変更。

### 2026-08-21 15:00
タイトル追加。
- UI上部に「最後の100円はこれだ!!」を追加し、CSSで配置・サイズを調整。

### 2026-08-21 15:30
レース番号表記の修正。
- プルダウンの表記を「第1R」→「1R」に変更。

### 2026-08-21 16:00
ルーレット回転演出をCSSスクロールアニメーションに変更。
- CSSアニメーションを使用して、数字が上下に流れる視覚効果を実装。

### 2026-08-21 16:30
スロット風スクロールアニメーションの実装。
- CSS/JSを刷新し、リールが滑らかに回転して停止する演出に変更。

### 2026-08-21 16:45
スロット演出の停止挙動の修正。
- CSS transitionとtranslateYを用いた滑らかな停止演出へ修正。

### 2026-08-21 17:00
ルーレット演出の微調整。
- アニメーション速度を「1s」に修正。
- .slot に box-sizing: border-box を追加。
- 停止位置計算を .item の高さ基準に変更し中央揃えを正確化。

### 2026-08-21 17:30
演出順序とタイトルの修正。
- 停止順を「右(3着)→中央(2着)→左(1着)」に修正。
- 停止待ち時間を「STARTから3秒、9秒、18秒」に修正。
- ページタイトルを「AIタカシの3連単予想」に変更。

### 2026-08-21 17:45
ルーレットのスクロール方向を「上→下」に変更。
- CSSアニメーションとJSの停止位置計算を調整し、滑らかな下向きスクロールと中央停止を実現。

### 2026-08-21 18:00
ルーレット停止演出の最適化。
- 停止時に逆走が発生しないようロジックを修正。
- transitionを用いて自然な減速停止を実現。
- ページタイトルを「AIタカシの3連単予想」に統一。

### 2026-08-21 18:30
スロット停止演出の物理挙動最適化。
- transitionを用いて自然な減速停止を実現。
- 回転方向(上→下)を維持しつつ、現在位置から前方にある目的数字への移動を計算して逆走を解消。
### 2026-08-21 19:00
ルーレット演出の物理挙動最適化。
- 上下スクロールの方向を「上→下」に統一。
- 停止時に現在位置から前方へ滑らかに減速停止するロジックを実装し、逆走を完全に解消。
- 1秒サイクル回転、停止順序、タイミングは変更なし。

### 2026-08-21 19:30
「AIタカシの3連単予想」へのタイトル変更、およびルーレット停止演出の完全最適化。
- ページタイトルを「AIタカシの3連単予想」に変更。
- ルーレットの回転を物理的に正しいスロット挙動（逆走防止、減速停止）に確定。
- 全機能の動作確認が完了し、プロジェクトを確定。

### 2026-08-22 10:00
README.mdに公開URLを追記。
- https://takashi7195.github.io/Project-002/

### 2026-08-22 10:30
ルーレットの表示不具合と挙動の修正。
- 数字非表示問題の解消（CSS変数と相対計算の導入）。
- ルーレット回転方向を「上から下へ」に統一。
- ページ初期表示時に各リールへ「1・2・3」を初期設定。
- 回転開始時にリールごとの位相（animation-delay）をずらし、回転開始時の機械的な同期を解消。
- 各修正において停止処理への影響がないことを検証済み。

### 2026-09-20 19:50
v0.1.11の競艇API→DB定期取り込み基盤を段階実装。
- `race_data`のキュー、期限付きリース、再試行、service_role限定RPCを追加。
- `race-ingest` Edge Functionを追加し、API取得・正規化・8レース単位のチャンク保存・日付単位の公開切替を実装。
- Project-012へCronを登録。当日5分ごと、過去backfill5分ごと、昨日分06:10 JSTの3系統で実行。
- 既存のGemini、コメント、ルーレットは変更していない。
- v0.1.10タグと切り戻し手順は維持。
### 2026-09-20 21:00
競艇APIデータの保存対象を、Supabase無料枠の容量を考慮して「当日を含む直近30日（当日から29日前まで）」へ変更。
- 7日分の実測後に30日分を段階取り込みする計画へ更新。
- 30日を超える履歴は今回の対象外とし、必要になった場合は容量・保存先・料金を再評価する。

### 2026-09-20 21:30
直近30日保存の実装を反映。
- `race-ingest` Edge Functionが実行日から29日前を下限としてバックフィルするよう変更。
- 古い環境変数が残っていても、30日より前へ範囲が拡大しないよう保護。
- 既存の30日より古いキューを無効化し、直近30日分を再キュー。
- 既存の古い保存済みデータは削除せず、削除は別途容量確認後に判断する。

### 2026-09-20 22:00
`race_data`をテスト用データとして初期化し、テーブル構造を保持したまま全行を削除。直近30日（2026-08-22〜2026-09-20）だけを再キューし、Cronを再開した。
- `public.comments`は保持。
- 初期化後の検証で、30日より前のレース行は0件。
- Cronは当日・バックフィル（5分ポーリング）・昨日分（日次）が稼働中。

### 2026-09-20 22:20
段階4の障害処理を補強。
- HTTP 429の`Retry-After`秒数をworkerの再試行時刻へ引き渡す処理を実装。
- 429の再試行時刻を確認するテストを追加し、全28テストが成功。
- `race-ingest` Edge Functionを再デプロイ。

### 2026-09-20 22:40
取得・失敗時の詳細ログを実装。
- 失敗時にもHTTP状態、取得サイズ、所要時間、ETag、Last-Modified、エラーコードを`race_data.ingestion_runs`へ保存するRPCを追加。
- 失敗ログの保存に失敗しても、タスクのリース解放を妨げない構造にした。
- 詳細ログのworker試験を追加し、全29テストが成功。
- `race-ingest` Edge Functionを再デプロイ。

### 2026-09-20 23:00
段階5の容量を予備測定。`race_data`のテーブル・索引合計は24,756,224 bytes（約23.6MiB）、レース792件、原本スナップショット7件、取得実行ログ184件だった。30日分の最終値ではないため、7日連続観測とバックフィル完了後に再測定する。詳細は`docs/RACE-DATA-CAPACITY-20260920.md`。

### 2026-09-20 23:20
条件付きHTTP取得を実装。
- 前回取得のETag / Last-Modifiedを日付単位で参照。
- 次回取得時に`If-None-Match` / `If-Modified-Since`を送信。
- 304応答を原本再保存なしで記録し、取得メタデータを保持。
- 成功時のHTTPメタデータをスナップショットの取り込み実行へ保存。
- 条件付き取得と304観測のテストを追加し、全30テストが成功。
- `race-ingest` Edge Functionを再デプロイ。

### 2026-09-20 23:40
受入試験を追加実施。DBキューのclaim、期限切れリース回収、完了後の残存確認、30日範囲を検証し合格。試験分離のため`race-ingestion/scheduler-db-smoke.sql`の試験タスクを優先度1000へ修正。詳細は`docs/RACE-DATA-ACCEPTANCE-20260920.md`。

### 2026-09-20 23:55
停止・再開の受入試験を追加。収集元を無効化した状態ではclaimされず、試験トランザクション内で再有効化されることを確認。

### 2026-09-21 00:10
一時テーブル方式の論理バックアップ・復元試験を実施。`races`、`race_entries`、`payouts`、`source_snapshots`の件数と日付範囲が一致し、実DBへの変更なしで合格。完全な別プロジェクト復元試験は未実施。

### 2026-09-21 00:30
7日連続観測に向けた容量観測を実装。`race_data.capacity_observations`、service_role限定RPC、毎日09:15 JSTのCronを追加し、初回観測（race_data約80.4MB、3,084レース、28スナップショット）を確認。

### 2026-09-21 00:50
段階7の管理用汎用検索を実装・検証。
- `race_data.search_current_races` と service_role限定の `public.race_data_search_current_races` を追加。
- day_headsが指す現行の`ready`/`published` batchだけを検索し、旧projectionの重複を除外。
- 日付範囲、競艇場、レース番号、艇番、選手名の組み合わせに対応し、出走表・展示・結果・払戻しを1行へまとめて返す。
- 対象レース数、通常払戻しの最大額と同額首位、最年少と同年齢行、結果確定レース数、coverage、`no_match`/`invalid_date_range`警告を返す。
- 実DBで全件上限、R/艇番絞り込み、選手名絞り込み、構造化行、最年少・結果集計、無効日付を確認。公開検索API・Gemini連携は次段階。
- 観測値と残制限を `docs/RACE-DATA-SEARCH-VALIDATION-20260920.md` に記録。

### 2026-09-21 01:10
汎用検索の追加条件を後方互換で実装。
- `race_data_search_current_races_filtered` を追加し、登録番号、級別、年齢範囲、賭式、払戻し範囲を組み合わせて検索可能にした。
- 既存の7引数 `race_data_search_current_races` は変更せず、両方ともservice_role限定。
- 実DBでA1、選手名、3連単かつ払戻し範囲の検索を各3件上限で確認。
- projection起点の級別/年齢、選手名、賭式/払戻し範囲用索引を追加し、実DBで3件の存在を確認。

### 2026-09-21 01:30
日付ごとのキュー・公開状態・出走表/展示/結果件数を返す`race_data_coverage_report`を追加。current head未作成を、キュー進行中の`pending`、隔離/無効化の`failed`、未追跡の`untracked`、成功後head欠落の`no_head_after_success`へ分類するよう更新。実DBの取得時点では30日中27日が正常、3日がpending、失敗0日だった。

### 2026-09-21 01:50
特定レースの過去スナップショットを追跡する`race_data_list_race_versions`を追加。semantic hash、batch状態、構成要素のpresence、projection ID、最新取り込みrunを上限付きで返し、実DBで11版中10版の取得と`no_match`を確認。

### 2026-09-21 02:10
現行検索結果へレースプログラムのタイトル・グレード・距離、展示時の天候・風・波、結果の決まり手・状態を追加。実DBで`program`、`preview`、`result`を確認した。
